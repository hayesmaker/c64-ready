name: Publish PROJECT_OVERVIEW to Wiki

on:
  push:
    branches: [ master ]

permissions:
  contents: write
  pages: write

jobs:
  publish-wiki:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Clone wiki (authenticated)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git clone https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.wiki.git wiki

      - name: Copy overview
        run: |
          mkdir -p wiki
          cp PROJECT_OVERVIEW.md wiki/Project_Overview.md

      - name: Commit and push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cd wiki
          git add Project_Overview.md || true
          git commit -m "Update Project Overview (auto)" || echo "no changes"
          # Try pushing to the wiki default branch. Some repos use `main`, others `master`.
          # Attempt `main` first, then `master`. If both fail, print a helpful message.
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.wiki.git HEAD:main || \
            git push https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.wiki.git HEAD:master || \
            (echo "Failed to push to wiki on both 'main' and 'master'. Check repository wiki default branch and token permissions." && exit 0)

