// This file was removed from the active source tree because it appears to be
// unused in the codebase. It has been archived here to preserve history
// and avoid accidental deletion. If you later decide to restore it,
// move it back into `src/emulator/` and re-export any required symbols.
//
// Search results showed no imports/usages of `src/emulator/memory.ts`.
// Keeping a small stub export so TypeScript treats this as a module.

export {};
